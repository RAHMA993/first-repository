#include <stdio.h>
#include <stdlib.h>

int main()
{
    float x, power;
    printf("Please Enter The Number\n");
    scanf("%f",&x);
     power= x*x;
     printf("The power of number %f is %f", x, power);
    return 0;
}
